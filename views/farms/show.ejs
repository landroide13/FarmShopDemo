<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= farm.name %></title>
</head>
<body>

    <h1><%= farm.name %></h1>
    <ul>
        <li>City: <%= farm.city %></li>
        <li>Email: <%= farm.email %></li>
    </ul>
    <h3>Products</h3>
    <ul>
        <% for(let p of farm.products){%>
            <li><a href="/products/<%= p._id %>"><%= p.name %></a></li>
       <% } %>     
    </ul>

    <form action="/farms/<%= farm._id %>?_method=DELETE" method="post">
        <button>Delete</button>
    </form>

    <br>
    <div class=""><a href="/farms/<%= farm._id %>/products/new">Add Product</a></div>
    </br>
    <div class=""><a href="/farms/<%= farm._id %>/edit">Edit</a></div>
    </br>
    <div class=""><a href="/farms">Back</a></div>
    </br>
    
</body>
</html>